# פריסה ל-GitHub Pages — הוראות

`_deploy_site/` = עותק **מוצפן** של קבצי הטיול (StatiCrypt · AES-256-CBC), מתפרסם ב-GitHub Pages.

> **נבנה מחדש 31/8/2026 (ערב) — אחרי איחוד הקבצים.** אותה סיסמה ואותו salt.
> **מה חדש:** כל יום ב-`ימים_מפורט/` הוא עכשיו קובץ שלם — ציר זמן, מפת מעבר, כל האוכל עם 112 קישורי Google Maps, כסף, תוכנית ב׳ וחלופת גשם. `01` הפך למפה (223KB במקום 434KB, בלי ציר זמן יומי) ו-`04` נבנה אוטומטית מקבצי הימים ע"י `_build/build_04.py`. `index.html` פוענח, עודכנו שני תיאורי אריחים ושורת הסטטוס, והוצפן מחדש.
> **נבדק בפועל:** 29/29 קבצים מפוענחים · סיסמה שגויה נדחית · 28/28 הקישורים ב-`index.html` מובילים לקובץ קיים.
>
> **נבנה מחדש 31/8/2026 (בוקר)** — אותה סיסמה ואותו salt כמו ב-22/8 (מי שסימן "Remember me" לא צריך להקליד מחדש).
> **מה חדש בבנייה הזאת:** נוספו `06_לוח_פעולות_ממוין` ו-`07_דוח_גאפים_31-08-2026` (שניהם לא היו באתר קודם) · לכל 20 הימים נוסף הסבר "מה עושים היום" וחלופה ליום גשום · `index.html` נבנה מחדש עם 8 אריחים במקום 6 וסטטוס מעודכן ל-31/8 · תוקנה שעת רכבת יום 9 (08:33) ורכבות יום 11 הופנו ל-e5489.
> **נבדק בפועל 31/8:** כל 29 הקבצים מפוענחים בהצלחה עם הסיסמה הקיימת · סיסמה שגויה נדחית · כל 28 הקישורים ב-`index.html` מובילים לקובץ קיים.
>
> **נבנה במקור 22/8/2026, סיסמה חדשה.** ההיסטוריה הישנה של הריפו נמחקה ונדחפה מחדש,
> והסיסמה הקודמת (שהייתה כתובה בקובץ הזה ונחשפה בריפו הציבורי) אינה בתוקף יותר.
>
> **נבדק בפועל:** הסיסמה החדשה פותחת את כל 7 סוגי הדפים · הסיסמה הישנה **נכשלת** ·
> סיסמה שגויה נכשלת ולא חושפת כלום · כל 26 הקישורים בדף הבית מובילים לקובץ קיים.

🔐 **הסיסמה וה-salt נמצאים ב-`LOCAL_SECRETS.md`** — הקובץ הזה מוחרג ב-`.gitignore` ולא נדחף.
**אף פעם לא לכתוב את הסיסמה בקובץ שנדחף לריפו.**

---

## 🔄 לעדכן את האתר

```
cd C:\work\private\claude-cowork\projects\JAP\_deploy_site
git add -A
git commit -m "Update site"
git push
```

בהתחברות: שם משתמש `meyals`, ובמקום סיסמה — **Personal Access Token**
(github.com → Settings → Developer settings → Personal access tokens, הרשאת `repo`).

אם git מתלונן על `index.lock` — `del .git\index.lock` ואז שוב.

אחרי דקה-שתיים: **https://meyals.github.io/JAP/**

---

## 📦 מה יש בתיקייה

| קובץ | מה זה |
|---|---|
| `index.html` | **דף הבית** — סטטוס נוכחי, 8 אריחים לקבצים הראשיים, ו-20 כרטיסי הימים |
| `01_תוכנית_רגועה_...html` | המפה: סטטוס, 20 כרטיסי ימים, 72 כרטיסי אתרים, רוחביים |
| `02_הזמנות_ומשימות.html` | לוח לפי תאריך + הזמנות לפי יום |
| `03_מלונות.html` | מלונות, סטטוס, דדליינים ותיק ההכרעה |
| `04_מדריך_אוכל.html` | מדריך האוכל — **קובץ מיוצר** מסעיפי האוכל של קבצי הימים |
| `05_דוח_ביקורת_26-07-2026.html` | דוח הביקורת מ-26/7 (היסטורי) |
| `06_לוח_פעולות_ממוין.html` | **לוח הפעולות** — 82 שורות, מיון לפי דחיפות/תאריך/יום |
| `07_דוח_גאפים_31-08-2026.html` | **דוח הגאפים מ-31/8** — 20 ממצאים + "מה לעשות לפי סדר" |
| `ימים_מפורט/` | **מקור האמת** — 20 קבצי יום, הכל בפנים |
| `_עזר/07_מפות_גוגל/` | ניווט מהיר ליום 3 |

**לא נכלל:** `00_קרא_אותי.md` (קובץ עבודה פנימי) · `_archive/` · ה-CSV של מפות יום 3.

---

## 🔁 לבנות מחדש (אחרי שינוי תוכן ב-`00_CURRENT`)

```
npm install -g staticrypt
staticrypt <תיקיית_מקור> -r -d <תיקיית_יעד> -p <הסיסמה> --remember 0 -s <ה-salt> -c false --short
```

הסיסמה וה-salt ב-`LOCAL_SECRETS.md`.

⚠️ **לשמור על אותו salt** — אחרת כל מי שסימן "Remember me" יצטרך להקליד סיסמה מחדש.
⚠️ **לפני בנייה: להריץ `python3 ../_build/build_04.py`** כדי ש-`04` יהיה מסונכרן עם קבצי הימים.
⚠️ **`index.html` אינו קובץ מ-`00_CURRENT`** — הוא דף בית שנבנה במיוחד לאתר. אם מוסיפים או משנים קבצים, צריך לעדכן גם אותו.

---

## 🔐 אבטחה

- לשלוח את הקישור ואת הסיסמה **בערוצים נפרדים** (קישור בוואטסאפ, סיסמה בשיחה) — כך הודעה אחת שדולפת לא מספיקה.
- GitHub Pages חינמי דורש ריפו **ציבורי**. זה בסדר **רק כל עוד הסיסמה לא בריפו** — התוכן מוצפן, ובלי הסיסמה אי אפשר לקרוא אותו.
- הסיסמה נשמרת אצל המבקר ב-localStorage אם סימן "Remember me". החלפת סיסמה מבטלת את זה לכולם.
