# פריסה ל-GitHub Pages — הוראות

`_deploy_site/` = עותק **מוצפן** של קבצי הטיול (StatiCrypt · AES-256-CBC), מתפרסם ב-GitHub Pages.

> **עודכן 12/9/2026 (מאוחר יותר) — פירוט הטואלטיקה האישית ברשימת האריזה.**
> **27 קבצים, קובץ אחד שונה (`06_רשימת_אריזה.html`).** סעיף הטואלטיקה פורק מ"ערכה" גנרית
> לפריטים נקובים שכל אחד מארבעת בני הבית ואייל אורז לעצמו: מברשת שיניים, משחה קטנה,
> מברשת שיער, גומיות, קרם/חומר לשיער, שפתון, בושם ודאודורנט. "פיג׳מה" שונה ל"בגדי שינה".
> שקיות הכביסה המלוכלכת שודרגו מ-2 ל-4 (מזוודה + תיק 3 הלילות + רזרבה) ושויכו לאייל.
> נוספו גם פריטים אישיים נקודתיים לבני משפחה מסוימים. שורת "מה לא לקחת" בנושא מברשות
> שיניים תוקנה — המלונות אמנם נותנים ערכה בכל לילה, אבל מברשת אישית אחת כן ברשימה.
> **נבדק בפועל:** Playwright — כל שמונת הפריטים מופיעים אצל כל אחד מהחמישה, אפס כפילויות
> בתוך אותו סעיף, אפס שגיאות JS. אחרי ההצפנה: 27/27 מפוענחים · 27/27 דוחים סיסמה שגויה ·
> תבנית StatiCrypt זהה ב-27/27 · 25/25 קישורי `index.html` תקינים · הפענוח של `06` זהה
> **בייט-לבייט** למקור ב-`00_CURRENT`. `index.html` ו-26 הקבצים האחרים לא שונו.
> **לא נדחף** — commit מקומי בלבד.
>
> **נבנה מחדש 12/9/2026 — רשימת אריזה (קובץ חדש) + סנכרון מלא.**
> **26 → 27 קבצים.** נוסף `06_רשימת_אריזה.html` — נפתח במסך "מי אורז עכשיו?" שבו כל אחד
> מחמשת בני המשפחה בוחר את שמו ורואה רק את הפריטים שלו (כ-58 לבת), עם טאב "משותף למשפחה",
> סימון שנשמר ב-`localStorage` וזיכרון של האדם האחרון שנבחר. בתוך הדף: איך מחלקים את
> 2 המזוודות, "תיק 3 הלילות" (24–27/9), חשבון 8 סטי הבגדים ושלוש הכביסות, ומה לא לקחת.
> `01`/`02`/`03`/`05` ו-20 קבצי הימים סונכרנו מ-`00_CURRENT` (עדכוני 12/9: ימים 8/9/10/12/15
> + לוח הפעולות + הקוין-לונדרי בטאקאיאמה + סגירת ההכרעה של יום 12). `03`/`05` נבנו מחדש
> עם `build_04.py`/`build_05_usj.py` — החיפוש החי של 10/9 שרד (הוא בתבנית, לא בקובץ).
> `index.html` פוענח ועודכן: **אריח חמישי** לרשימת האריזה, סטטוס תוארך ל-12/9, KPI
> "ימים להמראה" תוקן (7→2), ותאריכי הכותרת תוקנו מ-`15/9 – 5/10 · 19 ימים` ל-`14/9 – 4/10 ·
> 20 ימים` — הם סתרו את קבצי הימים שהם מקור האמת. לרשימת הפתוחים נוספו שני פריטים:
> קוין-לונדרי בטוקיו (חובה — אין coin laundry במלון) ואימות ביטולי MIMARU/soyo-ka.
> **נבדק בפועל:** 27/27 מפוענחים עם הסיסמה הנכונה · 27/27 דוחים סיסמה שגויה · תבנית
> StatiCrypt זהה ב-27/27 (md5 אחרי הסרת שורת ה-payload) · 25/25 קישורי `index.html` מובילים
> לקובץ קיים, כולל החדש · `index.html` המפוענח נטען ב-Playwright (דסקטופ + 390px): 5 אריחים,
> 20 כרטיסי ימים, אפס גלילה אופקית, אפס שגיאות JS. **לא נדחף** — commit מקומי בלבד.
>
> **נבנה מחדש 10/9/2026 — חיפוש חי במדריך האוכל.**
> **26 קבצים, קובץ אחד שונה (`03_מדריך_אוכל.html`).** נוספה תיבת חיפוש דביקה (sticky) בראש העמוד: מסננת חי לפי טקסט (שם מקום/מנה/יום/עיר), מסתירה ימים בלי התאמה ופותחת אוטומטית ימים עם תוצאה, עם מונה תוצאות וכפתור ניקוי. נוסף בתבנית הבנייה (`_build/04_head.html` + `_build/04_tail.html`) ולא ב-03 עצמו, כדי לשרוד בנייה מחדש. `00_CURRENT` נבנה מחדש עם `build_04.py` ואז הוצפן מחדש עם אותה סיסמה/salt.
> **נבדק בפועל:** Playwright (רוחב מובייל 390px) — חיפוש "Tsurutontan" מסנן ל-1 יום/1 שורה ופותח אותו אוטומטית, חיפוש בעברית ("אודון") מוצא 3 שורות ב-2 ימים, חיפוש ללא תוצאה מציג הודעה ומסתיר את כל הימים, ניקוי מחזיר הכל למצב ההתחלתי — אפס שגיאות JS. אחרי ההצפנה מחדש: 26/26 מפוענחים עם הסיסמה הנכונה · סיסמה שגויה נדחית (0 קבצים נוצרו) · תבנית StatiCrypt זהה ל-03 הקודם חוץ משורת ה-payload · פענוח 03 החדש זהה בייט-לבייט ל-`00_CURRENT` · 24/24 קישורי `index.html` תקינים. `index.html` לא שונה. **לא נדחף** — commit מקומי בלבד.
>
> **סונכרן 10/9/2026 — הסעה פרטית ליום 1 (Chinichi).**
> **26 קבצים.** `01_לוח_פעולות` ו-`ימים_מפורט/יום_1` היו שבע שעות מאחורי `00_CURRENT`
> (נערכו 8/9 17:26–17:28, סונכרן אחרון היה 8/9 10:38): הוזמנה הסעה פרטית מ-KIX ל-Cross
> Hotel Osaka (Chinichi דרך Booking.com, אישור #397797895, €72.16) — הפכה לתוכנית א׳
> ליום 1; רכבת ה-Rapi:t (ששולמה מראש) יורדת לגיבוי. עודכנו שני הקבצים בלבד, הוצפנו מחדש
> עם אותה סיסמה/salt. **נבדק בפועל:** 26/26 מפוענחים עם הסיסמה הנכונה · סיסמה שגויה
> נדחית · 25/25 קישורי `index.html` תקינים · שני הקבצים המפוענחים זהים בייט-לבייט
> למקור ב-`00_CURRENT`. `index.html`/`02`/`03`/`05` לא שונו — לא היה בהם פער.
>

> **נבנה מחדש 8/9/2026 (סבב שלישי) — טבלה אמיתית לרוחב מלא במקום כרטיסים.**
> **25 קבצים.** בעקבות פידבק שהכרטיסים (שדה-בשורה) לא היו מה שהתבקש — הוחלף CSS המובייל
> בכל 24 הקבצים הרלוונטיים (01/02/03 + 20 ימים) מ"כרטיסים מוערמים" לטבלה אמיתית לכל רוחב
> המסך: כותרות עמודות למעלה, כל השורה אופקית, פונט קטן וגלישת טקסט בתוך התא. נבדק בפועל
> ב-Playwright ברוחב 390px (לא רק חזותית) — אפס גלישה אופקית ברמת העמוד בכל 24 הקבצים.
> שתי טבלאות שנמדדו כצפופות מדי לכל הרוחב גם בפונט קטן (הלוח הראשי ב-01 — 7 עמודות; והשוואות
> המלונות `.all`/`.cmp` ב-02 — עד 9 עמודות עם טקסט חופשי) קיבלו גלילה אופקית פנימית מוכלת
> (לא גלילת עמוד) לפי בקשת המשתמש, אחרי שהוצג לו שהמתמטיקה לא מאפשרת הכל-ברוחב-אחד בלי
> אחת משתי הפשרות. תוקן גם באג `overflow-wrap:anywhere` שגרם לשבירת מילים לאות-אחר-אות
> בעמודות צרות, ובאג flexbox (`min-width:auto`) ב-02 שגרם לגלילת עמוד של 13px.
> **נבדק בפועל:** 26/26 מפוענחים עם הסיסמה הנכונה · סיסמה שגויה נדחית · 25/25 קישורי
> `index.html` תקינים · אפס גלישה אופקית ב-24/24 קבצים (Playwright, 390px). `index.html`
> לא שונה. **לא נדחף** — commit מקומי בלבד.
>
> **נבנה מחדש 8/9/2026 (עוד יותר מאוחר) — איחוד 01 + CSS מובייל לדוח גאפים.**
> **25 קבצים.** `01_לוח_פעולות`: הוסרו 8 תתי-טבלאות כפולות (`imported-02`, היו כמעט כולן שכפול
> של הטבלה הראשית) ורוכזו ל-83 שורות ברשימה אחת עם מיון/סינון כמו קודם, בלי תתי-מבנים; נוספו
> 4 פריטים אמיתיים שנמצאו חסרים באיחוד (לוחות זמנים אמיתיים, מסעדות בלי הזמנה, קופונים/הנחות,
> מיפוי 19 ה-holds), ותוקן eSIM (נוסף Mobal לרשימת הספקים). 3 הטבלאות בדוח-הגאפים
> (`imported-07`) קיבלו `data-label` + CSS למובייל — היו היחידות שנשארו עם גלילה אופקית אחרי
> הבנייה הקודמת. `02`/`03`/`05` וקבצי הימים סונכרנו מהמצב העדכני ב-`00_CURRENT` (`03`/`05`
> נבנו מחדש עם `build_04.py`/`build_05_usj.py`). `index.html` **לא שונה** — אין שינוי סטטוס/
> אריחים בסבב הזה.
> **נבדק בפועל:** 26/26 מפוענחים עם הסיסמה הנכונה (`staticrypt --decrypt`) · סיסמה שגויה
> נדחית · 25/25 קישורי `index.html` מובילים לקובץ קיים · תוכן נבדק ידנית מול `00_CURRENT`
> (Mobal, 83 שורות, CSS דוח-הגאפים, שורת קופונים חדשה). **לא נדחף** — commit מקומי בלבד.
>
> **נבנה מחדש 8/9/2026 (מאוחר יותר) — נגישות מובייל + הסרת אריח כפול.**
> **24 קבצים.** `01`/`02`/`03` וכל קבצי הימים (חוץ מ-17ב, בלי טבלת אוכל) קיבלו CSS למובייל
> שהופך טבלאות לכרטיסים במקום גלילה אופקית לא קריאה (`@media max-width:640px`, `data-label`
> על כל `<td>`). אותרו ותוקנו שני באגים מבניים שגרמו לגלישה שיורית גם אחרי ההמרה הראשונה
> (מ-1/9): חסר `tbody{display:block}`, ותגית `.diet` עם `white-space:nowrap` שגלשה כשהכילה
> משפט אזהרה ארוך במקום תווית קצרה. `02` (21 טבלאות מעורבות) טופל לפי כותרות ה-`<th>` של כל
> טבלה בנפרד; טבלאות `.kv` נשארו כפי שהן. `index.html`: הוסר אריח "ניווט מהיר · יום 3" הכפול
> (מיותר מאז שהניווט המהיר שולב בכל קובץ יום ב-4/9) — פוענח, נערך והוצפן מחדש, אותה סיסמה ואותו salt.
> **נבדק בפועל:** Playwright בוויופורט מובייל אמיתי (390px) — אפס גלישה אופקית בכל טבלה שנבדקה
> (01, 03 המלא, 02 המלא, מדגם קבצי יום). לאחר ההצפנה מחדש: 26/26 מפוענחים headless · סיסמה
> שגויה נדחית · 25/25 קישורי `index.html` תקינים (ירד מ-26 עם הסרת האריח) · 26/26 קבצים זהים
> לתבנית StatiCrypt חוץ משורת ה-payload. **לא נדחף** — commit מקומי בלבד.
>
> **נבנה מחדש 8/9/2026 — סנכרון תוכן שוטף (19 קבצי יום + לוח פעולות).**
> **26 קבצים, ללא שינוי במבנה.** `01_לוח_פעולות` ו-19 מתוך 20 קבצי `ימים_מפורט` עודכנו מעריכות שנעשו ב-00_CURRENT ב-7/9 (יום 0 ללא שינוי). `03` ו-`05` נבנו מחדש עם `build_04.py`/`build_05_usj.py` כרגיל. `index.html` פוענח, עודכן והוצפן מחדש — אותה סיסמה ואותו salt: תוקן ה-KPI 'ימים להמראה' (היה תקוע על 15 מ-31/8, עכשיו 7), סטטוס תוארך ל-8/9, ושני סעיפים בטופ הפתוחים סומנו כנסגרו (רכבות יום 11 ומלון טוקיו — שניהם הוזמנו בפועל לפי היומן הפרויקטלי). מייל Tenyu / החלטת DisneySea / ביטול MIMARU (9/9) נשארו פתוחים — אין אישור שנפתרו.
> **נבדק בפועל:** 26/26 מפוענחים בסקריפט headless · סיסמה שגויה נדחית · 26/26 הקישורים ב-`index.html` מובילים לקובץ קיים · כל 26 הקבצים השתנו בדיוק בשורת ה-payload (git diff --stat, לא נגעו במבנה). **לא נדחף** — commit מקומי בלבד.
>
> **נבנה מחדש 6/9/2026 — כרטיסי USJ + סנכרון 6 ימי שינויים.**
> **26 קבצים במקום 25.** נוסף `05_כרטיסי_USJ.html` — כרטיס כניסה (Studio Pass) + Express Pass 4 לכל אחד, משובצים כ-iframe מקודד base64 (בלי JavaScript, כדי לשמור על העקרון "עובד בלי JS"): אייל=1 · מיטל=2 · הדר=3 · לירון=4 · אלה=5. קישור אליו נוסף גם מ-`ימים_מפורט/יום_2` וגם מ-`index.html`.
> `01`/`02`/`03` ו-20 קבצי הימים עודכנו מהמצב העדכני ב-`00_CURRENT` (איחוד כפילויות במדריך האוכל, תיקון סתירת Motoi Gyoza, הזמנת מלון Takayama Green ומסעדת enen בפועל, Express Pass 4 מפורק לבחירות, ועוד). `index.html` פוענח, עודכן (אריח חדש + סטטוס 6/9) והוצפן מחדש — אותה סיסמה ואותו salt.
> **נבדק בפועל:** 26/26 מפוענחים בסקריפט headless (staticrypt) · סיסמה שגויה נדחית · תבנית StatiCrypt זהה ב-26/26 (רק שורת ה-payload משתנה) · 26/26 הקישורים ב-`index.html` מובילים לקובץ קיים · 10/10 קבצי הכרטיסים המקוריים (USJ/) עוברים round-trip בייטים-לבייטים לתוך `05`.
>
> **נבנה מחדש 31/8/2026 (לילה) — אחרי איחוד הקבצים המלא.** אותה סיסמה ואותו salt.
> **25 קבצים במקום 29.** שמונת הקבצים הראשיים אוחדו לשלושה: `01_לוח_פעולות` (היה 02+06+07 + הרוחביים של 01) · `02_מלונות` (היה 03) · `03_מדריך_אוכל` (היה 04, מיוצר). 72 כרטיסי האתרים ירדו לקבצי הימים. `05_דוח_ביקורת` ירד מהאתר לארכיון.
> **נבדק בפועל:** 25/25 מפוענחים · סיסמה שגויה נדחית · 24/24 הקישורים ב-`index.html` תקינים.
>
> **נבנה מחדש 31/8/2026 (ערב) — אחרי איחוד תוכן הימים.** אותה סיסמה ואותו salt.
> **מה חדש:** כל יום ב-`ימים_מפורט/` הוא עכשיו קובץ שלם — ציר זמן, מפת מעבר, כל האוכל עם 108 קישורי Google Maps, כסף, תוכנית ב׳ וחלופת גשם. `01` הפך למפה (223KB במקום 434KB, בלי ציר זמן יומי) ו-`04` נבנה אוטומטית מקבצי הימים ע"י `_build/build_04.py`. `index.html` פוענח, עודכנו שני תיאורי אריחים ושורת הסטטוס, והוצפן מחדש.
> **נבדק בפועל:** 29/29 קבצים מפוענחים · סיסמה שגויה נדחית · 28/28 הקישורים ב-`index.html` מובילים לקובץ קיים.
>
> **נבנה מחדש 31/8/2026 (בוקר)** — אותה סיסמה ואותו salt כמו ב-22/8 (מי שסימן "Remember me" לא צריך להקליד מחדש).
> **מה חדש בבנייה הזאת:** נוספו `06_לוח_פעולות_ממוין` ו-`07_דוח_גאפים_31-08-2026` (שניהם לא היו באתר קודם) · לכל 20 הימים נוסף הסבר "מה עושים היום" וחלופה ליום גשום · `index.html` נבנה מחדש עם 8 אריחים במקום 6 וסטטוס מעודכן ל-31/8 · תוקנה שעת רכבת יום 9 (08:33) ורכבות יום 11 הופנו ל-e5489.
> **נבדק בפועל 31/8:** כל 29 הקבצים מפוענחים בהצלחה עם הסיסמה הקיימת · סיסמה שגויה נדחית · כל 28 הקישורים ב-`index.html` מובילים לקובץ קיים.
>
> **נבנה במקור 22/8/2026, סיסמה חדשה.** ההיסטוריה הישנה של הריפו נמחקה ונדחפה מחדש,
> והסיסמה הקודמת (שהייתה כתובה בקובץ הזה ונחשפה בריפו הציבורי) אינה בתוקף יותר.
>
> **נבדק בפועל:** הסיסמה החדשה פותחת את כל 7 סוגי הדפים · הסיסמה הישנה **נכשלת** ·
> סיסמה שגויה נכשלת ולא חושפת כלום · כל 26 הקישורים בדף הבית מובילים לקובץ קיים.

🔐 **הסיסמה וה-salt נמצאים ב-`LOCAL_SECRETS.md`** — הקובץ הזה מוחרג ב-`.gitignore` ולא נדחף.
**אף פעם לא לכתוב את הסיסמה בקובץ שנדחף לריפו.**

---

## 🔄 לעדכן את האתר

```
cd C:\work\private\claude-cowork\projects\JAP\_deploy_site
git add -A
git commit -m "Update site"
git push
```

בהתחברות: שם משתמש `meyals`, ובמקום סיסמה — **Personal Access Token**
(github.com → Settings → Developer settings → Personal access tokens, הרשאת `repo`).

אם git מתלונן על `index.lock` — `del .git\index.lock` ואז שוב.

אחרי דקה-שתיים: **https://meyals.github.io/JAP/**

---

## 📦 מה יש בתיקייה

| קובץ | מה זה |
|---|---|
| `index.html` | **דף הבית** — סטטוס, 4 אריחים, ו-20 כרטיסי הימים. **בנוי במיוחד לאתר** |
| `01_לוח_פעולות.html` | כל המשימות והדדליינים, ממוין וניתן לסינון + הזמנות לפי תאריך/יום + דוח גאפים + סיכונים והחלטות |
| `02_מלונות.html` | מלונות, סטטוס, דדליינים ותיק ההכרעה |
| `03_מדריך_אוכל.html` | מדריך האוכל — **קובץ מיוצר** מסעיפי האוכל של קבצי הימים |
| `05_כרטיסי_USJ.html` | כרטיסי USJ (כניסה + Express), זוג לכל אדם — **קובץ מיוצר** מ-`00_CURRENT/USJ/` |
| `ימים_מפורט/` | **מקור האמת** — 20 קבצי יום, הכל בפנים |
| `_עזר/07_מפות_גוגל/` | ניווט מהיר ליום 3 |

**לא נכלל:** `00_קרא_אותי.md` (קובץ עבודה פנימי) · `_archive/` · ה-CSV של מפות יום 3.

---

## 🔁 לבנות מחדש (אחרי שינוי תוכן ב-`00_CURRENT`)

```
npm install -g staticrypt
staticrypt <תיקיית_מקור> -r -d <תיקיית_יעד> -p <הסיסמה> --remember 0 -s <ה-salt> -c false --short
```

הסיסמה וה-salt ב-`LOCAL_SECRETS.md`.

⚠️ **לשמור על אותו salt** — אחרת כל מי שסימן "Remember me" יצטרך להקליד סיסמה מחדש.
⚠️ **לפני בנייה: להריץ `python3 ../_build/build_04.py`** כדי ש-`04` יהיה מסונכרן עם קבצי הימים.
⚠️ **וגם: `python3 ../_build/build_05_usj.py`** כדי ש-`05_כרטיסי_USJ` יהיה מסונכרן עם `00_CURRENT/USJ/`.
⚠️ **`index.html` אינו קובץ מ-`00_CURRENT`** — הוא דף בית שנבנה במיוחד לאתר. אם מוסיפים או משנים קבצים, צריך לעדכן גם אותו.

---

## 🔐 אבטחה

- לשלוח את הקישור ואת הסיסמה **בערוצים נפרדים** (קישור בוואטסאפ, סיסמה בשיחה) — כך הודעה אחת שדולפת לא מספיקה.
- GitHub Pages חינמי דורש ריפו **ציבורי**. זה בסדר **רק כל עוד הסיסמה לא בריפו** — התוכן מוצפן, ובלי הסיסמה אי אפשר לקרוא אותו.
- הסיסמה נשמרת אצל המבקר ב-localStorage אם סימן "Remember me". החלפת סיסמה מבטלת את זה לכולם.
