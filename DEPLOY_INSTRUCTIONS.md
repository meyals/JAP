# פריסה ל-GitHub Pages — הוראות

התיקייה הזו (`_deploy_site/`) היא **עותק מוצפן ומוכן לפרסום** של קבצי הטיול.
כל קבצי ה-HTML מוצפנים בפועל (StatiCrypt · AES-256-CBC).

> **נבנה מחדש: 22/8/2026.** הגרסה הקודמת הייתה מ-10/8/2026 והכילה את מבנה הקבצים הישן
> (`04_מדריך_מסעדות`, `05_מלונות`, `03_TODO`) שכבר לא קיים.
>
> **נבדק בפועל:** סיסמה נכונה משחזרת את התוכן במלואו · סיסמה שגויה נכשלת ולא חושפת כלום ·
> כל 26 הקישורים בדף הבית מובילים לקובץ קיים.

---

## 🔴 לקרוא לפני ה-push הבא

הסיסמה **הייתה כתובה בקובץ הזה** בגרסה הקודמת — והקובץ הזה נדחף לריפו **הציבורי**.
כלומר כל מי שמצא את הריפו יכול היה לפתוח את התוכן. **הסיסמה הישנה נחשפת גם בהיסטוריית ה-git,
גם אחרי שמסירים אותה מהקובץ.**

הסיסמה הוסרה מהקובץ הזה. כדי לסגור את זה עד הסוף צריך **גם** להחליף סיסמה **וגם** להיפטר
מההיסטוריה הישנה — הכי פשוט: למחוק את הריפו ב-GitHub וליצור אותו מחדש, ואז:

```
cd C:\work\private\claude-cowork\projects\JAP\_deploy_site
rmdir /s /q .git
git init && git add -A && git commit -m "Japan trip site" && git branch -M main
git remote add origin https://github.com/meyals/JAP.git
git push -u origin main --force
```

הסיסמה נמצאת ב-`LOCAL_SECRETS.md` — הקובץ הזה **מוחרג ב-.gitignore ולא נדחף**.

---

## 🔄 לעדכן את האתר

הריפו כבר מחובר (`origin` → `https://github.com/meyals/JAP.git`).

```
cd C:\work\private\claude-cowork\projects\JAP\_deploy_site
git add -A
git commit -m "Rebuild site from 2026-08-22 reorg"
git push
```

בהתחברות תתבקש Personal Access Token במקום סיסמה
(github.com → Settings → Developer settings → Personal access tokens).

אחרי דקה-שתיים האתר מתעדכן ב: **https://meyals.github.io/JAP/**

---

## 📦 מה יש בתיקייה

| קובץ | מה זה |
|---|---|
| `index.html` | **דף הבית** — סטטוס נוכחי, 6 אריחים לקבצים הראשיים, ו-20 כרטיסי הימים |
| `01_תוכנית_רגועה_...html` | התוכנית המלאה |
| `02_הזמנות_ומשימות.html` | לוח לפי תאריך + הזמנות לפי יום |
| `03_מלונות.html` | מלונות, סטטוס, דדליינים ותיק ההכרעה |
| `04_מדריך_אוכל.html` | מדריך האוכל, עם קישורי מפה ותגיות מגבלות אוכל |
| `05_דוח_ביקורת_26-07-2026.html` | דוח הביקורת |
| `ימים_מפורט/` | 20 מדריכי יום מפורטים |
| `_עזר/07_מפות_גוגל/` | ניווט מהיר ליום 3 |

**לא נכלל באתר:** `00_קרא_אותי.md` (קובץ עבודה פנימי) · `_archive/` · קובץ ה-CSV של מפות יום 3.

---

## 🔁 איך לבנות מחדש בפעם הבאה

הקבצים המקוריים (הלא-מוצפנים) יושבים ב-`00_CURRENT/`:

```
npm install -g staticrypt
staticrypt <תיקיית_מקור> -r -d <תיקיית_יעד> -p <הסיסמה> --remember 0 -s <ה-salt> -c false --short
```

הסיסמה וה-salt נמצאים ב-`LOCAL_SECRETS.md`.

⚠️ **לשמור על אותו salt** — זה מה שמאפשר למי שסימן "Remember me" להמשיך להיכנס בלי להקליד מחדש.
⚠️ **`index.html` אינו קובץ מ-`00_CURRENT`** — הוא דף בית שנבנה במיוחד לאתר. אם מוסיפים או משנים קבצים, צריך לעדכן גם אותו.

---

## 🔐 אבטחה

- לשלוח את הקישור ואת הסיסמה **בערוצים נפרדים** (קישור בוואטסאפ, סיסמה בשיחה) — כך הודעה אחת שדולפת לא מספיקה.
- GitHub Pages חינמי דורש ריפו **ציבורי**. זה בסדר **רק כל עוד הסיסמה עצמה לא בריפו** — התוכן מוצפן בקובץ, אז בלי הסיסמה אי אפשר לקרוא אותו.
- אף פעם לא לכתוב את הסיסמה בקובץ שנמצא בתוך `_deploy_site/` בלי להחריג אותו ב-`.gitignore`.
